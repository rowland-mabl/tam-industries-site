import React from 'react';
import ReactDOM from 'react-dom/client';
import TAMIndustries from './TAMIndustries';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TAMIndustries />
  </React.StrictMode>
);